@{
    ViewBag.Title = "About";
}

@if (User.Identity.IsAuthenticated)
{
    <h4>Welcome User @(User.Identity.IsAuthenticated ? HttpContext.Current.User.Identity.Name : "Guest") to be a Manager</h4>
}
@*<h2>@ViewBag.Title.</h2>*@

<h3>@ViewBag.Message</h3>

<p>Use this area to provide additional information.</p>
